version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/vrshow
hooks:
  ApplicationStart:
    - location: bin/ApplicationStart.sh
      timeout: 180
  AfterInstall:
    - location: bin/AfterInstall.sh
      timeout: 180000
